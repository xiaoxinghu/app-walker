var app = require('../../lib/appwalker');

describe('compiler', () => {
  // beforeEach(() => {
  //   app.reset();
  //   graph = app.graph;
  // });
  // afterEach(() => {
  //   app.reset();
  // });
});
